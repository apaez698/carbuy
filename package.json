{
  "name": "vendoya",
  "version": "1.0.0",
  "description": "VendoYa - Vende tu auto en Quito",
  "type": "module",
  "scripts": {
    "dev": "cd app && npm run dev",
    "test": "vitest run tests",
    "test:watch": "vitest tests"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
